services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./artifacts/hybrid_retriever_e5:/app/artifacts/hybrid_retriever_e5:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
